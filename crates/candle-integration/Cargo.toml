[package]
name = "reimagine-candle-integration"
version.workspace = true
description.workspace = true
authors.workspace = true
edition.workspace = true

[dependencies]
reimagine-core = { path = "../core" }
# Candle dependencies (candle-core, candle-nn, candle-transformers, hf-hub) added at M1.